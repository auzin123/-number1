# append -  присоеденяет в конец
# pop - удаляет или возвращает элемент
# in - есть ли элемент в списке
# sort - сортируем список
# сортируем только однородные (type) списки


from  random import randint
random_digits = []
for i in range(10):
    random_digits.append(randint(0, 9))


print(random_digits)